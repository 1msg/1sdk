/**
 * one_msg_waba_sdk
 * [1MSG.IO](https://1msg.io/) is the perfect WhatsApp management tool for your business. With us you get full access to the official Whatsapp API/webhooks.  Every API request must contain an Authorize HTTP header with a token.  This is your channel token, which can be found in your channel project on your profile page. Please do not give the token to anyone or post it publicly.  The authorization token must be added to each request in the GET parameter 'token' and always passed to query string (?token={your_token}). Parameters in GET queries pass query string. Parameters in POST requests — through the JSON-encoded request body.   All 'send' methods (except /sendTemplate) will only work when the dialog session with the user is open. Some of our solutions simplify and avoid such limitations, but we urge you to pay more attention to this detail
 *
 * The version of the OpenAPI document: 2.39.0
 * Contact: hello@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TemplateComponentsPropInnerButtonsInner model module.
 * @module model/TemplateComponentsPropInnerButtonsInner
 * @version 2.39.0
 */
class TemplateComponentsPropInnerButtonsInner {
    /**
     * Constructs a new <code>TemplateComponentsPropInnerButtonsInner</code>.
     * @alias module:model/TemplateComponentsPropInnerButtonsInner
     */
    constructor() { 
        
        TemplateComponentsPropInnerButtonsInner.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>TemplateComponentsPropInnerButtonsInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TemplateComponentsPropInnerButtonsInner} obj Optional instance to populate.
     * @return {module:model/TemplateComponentsPropInnerButtonsInner} The populated <code>TemplateComponentsPropInnerButtonsInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TemplateComponentsPropInnerButtonsInner();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('phone_number')) {
                obj['phone_number'] = ApiClient.convertToType(data['phone_number'], 'String');
            }
            if (data.hasOwnProperty('example')) {
                obj['example'] = ApiClient.convertToType(data['example'], [Object]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TemplateComponentsPropInnerButtonsInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TemplateComponentsPropInnerButtonsInner</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['text'] && !(typeof data['text'] === 'string' || data['text'] instanceof String)) {
            throw new Error("Expected the field `text` to be a primitive type in the JSON string but got " + data['text']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // ensure the json data is a string
        if (data['phone_number'] && !(typeof data['phone_number'] === 'string' || data['phone_number'] instanceof String)) {
            throw new Error("Expected the field `phone_number` to be a primitive type in the JSON string but got " + data['phone_number']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['example'])) {
            throw new Error("Expected the field `example` to be an array in the JSON data but got " + data['example']);
        }

        return true;
    }


}



/**
 * @member {module:model/TemplateComponentsPropInnerButtonsInner.TypeEnum} type
 */
TemplateComponentsPropInnerButtonsInner.prototype['type'] = undefined;

/**
 * @member {String} text
 */
TemplateComponentsPropInnerButtonsInner.prototype['text'] = undefined;

/**
 * @member {String} url
 */
TemplateComponentsPropInnerButtonsInner.prototype['url'] = undefined;

/**
 * @member {String} phone_number
 */
TemplateComponentsPropInnerButtonsInner.prototype['phone_number'] = undefined;

/**
 * @member {Array.<Object>} example
 */
TemplateComponentsPropInnerButtonsInner.prototype['example'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
TemplateComponentsPropInnerButtonsInner['TypeEnum'] = {

    /**
     * value: "PHONE_NUMBER"
     * @const
     */
    "PHONE_NUMBER": "PHONE_NUMBER",

    /**
     * value: "URL"
     * @const
     */
    "URL": "URL",

    /**
     * value: "QUICK_REPLY"
     * @const
     */
    "QUICK_REPLY": "QUICK_REPLY"
};



export default TemplateComponentsPropInnerButtonsInner;

