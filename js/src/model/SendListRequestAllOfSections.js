/**
 * one_msg_waba_sdk
 * [1MSG.IO](https://1msg.io/) is the perfect WhatsApp management tool for your business. With us you get full access to the official Whatsapp API/webhooks.  Every API request must contain an Authorize HTTP header with a token.  This is your channel token, which can be found in your channel project on your profile page. Please do not give the token to anyone or post it publicly.  The authorization token must be added to each request in the GET parameter 'token' and always passed to query string (?token={your_token}). Parameters in GET queries pass query string. Parameters in POST requests — through the JSON-encoded request body.   All 'send' methods (except /sendTemplate) will only work when the dialog session with the user is open. Some of our solutions simplify and avoid such limitations, but we urge you to pay more attention to this detail
 *
 * The version of the OpenAPI document: 2.39.0
 * Contact: hello@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SendListRequestAllOfRows from './SendListRequestAllOfRows';

/**
 * The SendListRequestAllOfSections model module.
 * @module model/SendListRequestAllOfSections
 * @version 2.39.0
 */
class SendListRequestAllOfSections {
    /**
     * Constructs a new <code>SendListRequestAllOfSections</code>.
     * @alias module:model/SendListRequestAllOfSections
     * @param rows {Array.<module:model/SendListRequestAllOfRows>} 
     */
    constructor(rows) { 
        
        SendListRequestAllOfSections.initialize(this, rows);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, rows) { 
        obj['rows'] = rows;
    }

    /**
     * Constructs a <code>SendListRequestAllOfSections</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SendListRequestAllOfSections} obj Optional instance to populate.
     * @return {module:model/SendListRequestAllOfSections} The populated <code>SendListRequestAllOfSections</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SendListRequestAllOfSections();

            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('rows')) {
                obj['rows'] = ApiClient.convertToType(data['rows'], [SendListRequestAllOfRows]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SendListRequestAllOfSections</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SendListRequestAllOfSections</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SendListRequestAllOfSections.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        if (data['rows']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rows'])) {
                throw new Error("Expected the field `rows` to be an array in the JSON data but got " + data['rows']);
            }
            // validate the optional field `rows` (array)
            for (const item of data['rows']) {
                SendListRequestAllOfRows.validateJSON(item);
            };
        }

        return true;
    }


}

SendListRequestAllOfSections.RequiredProperties = ["rows"];

/**
 * Title of section, up to 24 symbols. Required if there are more then 1 section
 * @member {String} title
 */
SendListRequestAllOfSections.prototype['title'] = undefined;

/**
 * @member {Array.<module:model/SendListRequestAllOfRows>} rows
 */
SendListRequestAllOfSections.prototype['rows'] = undefined;






export default SendListRequestAllOfSections;

